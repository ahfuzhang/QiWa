
* 第 1 次, qps: 302416.15

```
run:
	$(BIN_DIR)/$(BINARY) \
	    -thread.count=8 \
		-connection.count=16 \
		-max.concurrent.streams=50 \
		-target.url=http://localhost:9081/echo \
		-duration.seconds=10 \
		-check.output=false

```

* 第 1 次, qps: 303205.74

```
run:
	$(BIN_DIR)/$(BINARY) \
	    -thread.count=8 \
		-connection.count=16 \
		-max.concurrent.streams=100 \
		-target.url=http://localhost:9081/echo \
		-duration.seconds=10 \
		-check.output=false
```

